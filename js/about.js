$(function() {
  $('#tabs').tabs();
});

$(document).ready(function() {
    $('#mnuAbout').addClass('active');

    /*
    if( /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) ) {
        // hide tabs on mobile site
        $tab_div = $('.tab_content');
        $($tab_div).hide();
    }
    */
});